import { styled } from "@material-ui/core";

export const Container = styled('section')(() => ({

}));

export const ContentNewLaunch = styled('section')(() => ({

}));

export const ContentAllEpisodes = styled('section')(() => ({

}));

export const Title = styled('section')(() => ({

}));

export const CardNewEpisode = styled('section')(() => ({

}));

export const Card = styled('section')(() => ({

}));

export const RenderNewLaunch = styled('section')(() => ({

}));

export const RenderAllEpisodes = styled('section')(() => ({

}));